pipeline {
    agent any

    stages {

        stage('Clean') {
            steps {
                dotnet clean
            }
        }

        stage('Build') {
            steps {
                dotnet build
            }
        }
    }
}